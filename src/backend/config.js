const BASE_URL = "https://92171abb37f9.ngrok.app";

// Update interval in milliseconds (e.g., 60 minutes)
const UPDATE_INTERVAL = 1 * 60 * 1000; // 60 minutes

// Fetch interval in milliseconds (e.g., 300 minutes)
const FETCH_INTERVAL = 1 * 60 * 1000; // 300 minutes

export { BASE_URL, UPDATE_INTERVAL, FETCH_INTERVAL };
