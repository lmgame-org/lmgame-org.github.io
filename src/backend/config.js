const BASE_URL = "https://0e2d-70-95-168-38.ngrok-free.app";

// Update interval in milliseconds (e.g., 60 minutes)
const UPDATE_INTERVAL = 60; // 60s

// Fetch interval in milliseconds (e.g., 30 seconds)
const FETCH_INTERVAL = 30 * 1000; // 30 seconds

export { BASE_URL, UPDATE_INTERVAL, FETCH_INTERVAL };
